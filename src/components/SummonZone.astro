---
export interface Props {
  position: 'top' | 'bottom';
}

const { position } = Astro.props;
---
<div class="summon-zone" data-summon-zone={position}>
  <button type="button" class="summon-zone__btn" data-summon-trigger={position}>
    bring back the card
  </button>
</div>
